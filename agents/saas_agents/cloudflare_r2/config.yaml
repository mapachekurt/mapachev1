agent_name: "cloudflare_r2_agent"
display_name: "Cloudflare R2"
description: "Expert agent for Cloudflare R2 operations and integration"
version: "1.0.0"
model: "gemini-2.0-flash-exp"
tier: "Productivity & Collaboration"
category: "file_sharing"

capabilities:
  - api_integration
  - data_synchronization
  - workflow_automation
  - real_time_operations
  - error_handling
  - security_management

authentication:
  type: "oauth"  # Placeholder for future OAuth migration
  current: "api_key"
  env_var: "CLOUDFLARE_R2_API_KEY"

mcp_server:
  available: false
  source: "custom"

api_config:
  base_url: "https://api.cloudflarer2.com"
  rate_limit: 1000  # requests per hour
  docs_url: "https://docs.cloudflarer2.com"
  version: "v1"

deployment:
  dev:
    endpoint: "dev-cloudflare_r2-agent"
    project: "mapache-dev"
  staging:
    endpoint: "staging-cloudflare_r2-agent"
    project: "mapache-staging"
  prod:
    endpoint: "prod-cloudflare_r2-agent"
    project: "mapache-prod"

monitoring:
  health_check: true
  metrics: true
  logging: "structured_json"
  alerts: true

integration_status:
  implemented: false
  tested: false
  deployed: false
  production_ready: false