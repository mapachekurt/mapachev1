agent_name: "ranktracker_agent"
display_name: "Rank Tracker"
description: "Expert agent for Rank Tracker operations and integration"
version: "1.0.0"
model: "gemini-2.0-flash-exp"
tier: "Marketing & Sales"
category: "seo"

capabilities:
  - api_integration
  - data_synchronization
  - workflow_automation
  - real_time_operations
  - error_handling
  - security_management

authentication:
  type: "oauth"  # Placeholder for future OAuth migration
  current: "api_key"
  env_var: "RANKTRACKER_API_KEY"

mcp_server:
  available: false
  source: "custom"

api_config:
  base_url: "https://api.ranktracker.com"
  rate_limit: 1000  # requests per hour
  docs_url: "https://docs.ranktracker.com"
  version: "v1"

deployment:
  dev:
    endpoint: "dev-ranktracker-agent"
    project: "mapache-dev"
  staging:
    endpoint: "staging-ranktracker-agent"
    project: "mapache-staging"
  prod:
    endpoint: "prod-ranktracker-agent"
    project: "mapache-prod"

monitoring:
  health_check: true
  metrics: true
  logging: "structured_json"
  alerts: true

integration_status:
  implemented: false
  tested: false
  deployed: false
  production_ready: false