agent_name: "aws_s3_agent"
display_name: "AWS S3"
description: "Expert agent for AWS S3 operations and integration"
version: "1.0.0"
model: "gemini-2.0-flash-exp"
tier: "Developer Tools"
category: "cloud"

capabilities:
  - api_integration
  - data_synchronization
  - workflow_automation
  - real_time_operations
  - error_handling
  - security_management

authentication:
  type: "oauth"  # Placeholder for future OAuth migration
  current: "api_key"
  env_var: "AWS_S3_API_KEY"

mcp_server:
  available: false
  source: "custom"

api_config:
  base_url: "https://api.awss3.com"
  rate_limit: 1000  # requests per hour
  docs_url: "https://docs.awss3.com"
  version: "v1"

deployment:
  dev:
    endpoint: "dev-aws_s3-agent"
    project: "mapache-dev"
  staging:
    endpoint: "staging-aws_s3-agent"
    project: "mapache-staging"
  prod:
    endpoint: "prod-aws_s3-agent"
    project: "mapache-prod"

monitoring:
  health_check: true
  metrics: true
  logging: "structured_json"
  alerts: true

integration_status:
  implemented: false
  tested: false
  deployed: false
  production_ready: false