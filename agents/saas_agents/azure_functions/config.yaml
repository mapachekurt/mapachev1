agent_name: "azure_functions_agent"
display_name: "Azure Functions"
description: "Expert agent for Azure Functions operations and integration"
version: "1.0.0"
model: "gemini-2.0-flash-exp"
tier: "Developer Tools"
category: "cloud"

capabilities:
  - api_integration
  - data_synchronization
  - workflow_automation
  - real_time_operations
  - error_handling
  - security_management

authentication:
  type: "oauth"  # Placeholder for future OAuth migration
  current: "api_key"
  env_var: "AZURE_FUNCTIONS_API_KEY"

mcp_server:
  available: false
  source: "custom"

api_config:
  base_url: "https://api.azurefunctions.com"
  rate_limit: 1000  # requests per hour
  docs_url: "https://docs.azurefunctions.com"
  version: "v1"

deployment:
  dev:
    endpoint: "dev-azure_functions-agent"
    project: "mapache-dev"
  staging:
    endpoint: "staging-azure_functions-agent"
    project: "mapache-staging"
  prod:
    endpoint: "prod-azure_functions-agent"
    project: "mapache-prod"

monitoring:
  health_check: true
  metrics: true
  logging: "structured_json"
  alerts: true

integration_status:
  implemented: false
  tested: false
  deployed: false
  production_ready: false