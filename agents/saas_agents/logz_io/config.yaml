agent_name: "logz_io_agent"
display_name: "Logz.io"
description: "Expert agent for Logz.io operations and integration"
version: "1.0.0"
model: "gemini-2.0-flash-exp"
tier: "Developer Tools"
category: "monitoring"

capabilities:
  - api_integration
  - data_synchronization
  - workflow_automation
  - real_time_operations
  - error_handling
  - security_management

authentication:
  type: "oauth"  # Placeholder for future OAuth migration
  current: "api_key"
  env_var: "LOGZ_IO_API_KEY"

mcp_server:
  available: false
  source: "custom"

api_config:
  base_url: "https://api.logzio.com"
  rate_limit: 1000  # requests per hour
  docs_url: "https://docs.logzio.com"
  version: "v1"

deployment:
  dev:
    endpoint: "dev-logz_io-agent"
    project: "mapache-dev"
  staging:
    endpoint: "staging-logz_io-agent"
    project: "mapache-staging"
  prod:
    endpoint: "prod-logz_io-agent"
    project: "mapache-prod"

monitoring:
  health_check: true
  metrics: true
  logging: "structured_json"
  alerts: true

integration_status:
  implemented: false
  tested: false
  deployed: false
  production_ready: false