agent_name: "insightly_agent"
display_name: "Insightly"
description: "Expert agent for Insightly operations and integration"
version: "1.0.0"
model: "gemini-2.0-flash-exp"
tier: "Marketing & Sales"
category: "crm"

capabilities:
  - api_integration
  - data_synchronization
  - workflow_automation
  - real_time_operations
  - error_handling
  - security_management

authentication:
  type: "oauth"  # Placeholder for future OAuth migration
  current: "api_key"
  env_var: "INSIGHTLY_API_KEY"

mcp_server:
  available: false
  source: "custom"

api_config:
  base_url: "https://api.insightly.com"
  rate_limit: 1000  # requests per hour
  docs_url: "https://docs.insightly.com"
  version: "v1"

deployment:
  dev:
    endpoint: "dev-insightly-agent"
    project: "mapache-dev"
  staging:
    endpoint: "staging-insightly-agent"
    project: "mapache-staging"
  prod:
    endpoint: "prod-insightly-agent"
    project: "mapache-prod"

monitoring:
  health_check: true
  metrics: true
  logging: "structured_json"
  alerts: true

integration_status:
  implemented: false
  tested: false
  deployed: false
  production_ready: false