agent_name: "vend_agent"
display_name: "Vend (Lightspeed Retail)"
description: "Expert agent for Vend (Lightspeed Retail) operations and integration"
version: "1.0.0"
model: "gemini-2.0-flash-exp"
tier: "Specialized Vertical Tools"
category: "retail"

capabilities:
  - api_integration
  - data_synchronization
  - workflow_automation
  - real_time_operations
  - error_handling
  - security_management

authentication:
  type: "oauth"  # Placeholder for future OAuth migration
  current: "api_key"
  env_var: "VEND_API_KEY"

mcp_server:
  available: false
  source: "custom"

api_config:
  base_url: "https://api.vend.com"
  rate_limit: 1000  # requests per hour
  docs_url: "https://docs.vend.com"
  version: "v1"

deployment:
  dev:
    endpoint: "dev-vend-agent"
    project: "mapache-dev"
  staging:
    endpoint: "staging-vend-agent"
    project: "mapache-staging"
  prod:
    endpoint: "prod-vend-agent"
    project: "mapache-prod"

monitoring:
  health_check: true
  metrics: true
  logging: "structured_json"
  alerts: true

integration_status:
  implemented: false
  tested: false
  deployed: false
  production_ready: false