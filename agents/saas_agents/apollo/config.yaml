agent_name: "apollo_agent"
display_name: "Apollo.io"
description: "Expert agent for Apollo.io operations and integration"
version: "1.0.0"
model: "gemini-2.0-flash-exp"
tier: "Marketing & Sales"
category: "lead_generation"

capabilities:
  - api_integration
  - data_synchronization
  - workflow_automation
  - real_time_operations
  - error_handling
  - security_management

authentication:
  type: "oauth"  # Placeholder for future OAuth migration
  current: "api_key"
  env_var: "APOLLO_API_KEY"

mcp_server:
  available: false
  source: "custom"

api_config:
  base_url: "https://api.apollo.com"
  rate_limit: 1000  # requests per hour
  docs_url: "https://docs.apollo.com"
  version: "v1"

deployment:
  dev:
    endpoint: "dev-apollo-agent"
    project: "mapache-dev"
  staging:
    endpoint: "staging-apollo-agent"
    project: "mapache-staging"
  prod:
    endpoint: "prod-apollo-agent"
    project: "mapache-prod"

monitoring:
  health_check: true
  metrics: true
  logging: "structured_json"
  alerts: true

integration_status:
  implemented: false
  tested: false
  deployed: false
  production_ready: false