agent_name: "epicor_agent"
display_name: "Epicor ERP"
description: "Expert agent for Epicor ERP operations and integration"
version: "1.0.0"
model: "gemini-2.0-flash-exp"
tier: "Specialized Vertical Tools"
category: "manufacturing"

capabilities:
  - api_integration
  - data_synchronization
  - workflow_automation
  - real_time_operations
  - error_handling
  - security_management

authentication:
  type: "oauth"  # Placeholder for future OAuth migration
  current: "api_key"
  env_var: "EPICOR_API_KEY"

mcp_server:
  available: false
  source: "custom"

api_config:
  base_url: "https://api.epicor.com"
  rate_limit: 1000  # requests per hour
  docs_url: "https://docs.epicor.com"
  version: "v1"

deployment:
  dev:
    endpoint: "dev-epicor-agent"
    project: "mapache-dev"
  staging:
    endpoint: "staging-epicor-agent"
    project: "mapache-staging"
  prod:
    endpoint: "prod-epicor-agent"
    project: "mapache-prod"

monitoring:
  health_check: true
  metrics: true
  logging: "structured_json"
  alerts: true

integration_status:
  implemented: false
  tested: false
  deployed: false
  production_ready: false