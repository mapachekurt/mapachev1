agent_name: "top_producer_agent"
display_name: "Top Producer"
description: "Expert agent for Top Producer operations and integration"
version: "1.0.0"
model: "gemini-2.0-flash-exp"
tier: "Specialized Vertical Tools"
category: "real_estate"

capabilities:
  - api_integration
  - data_synchronization
  - workflow_automation
  - real_time_operations
  - error_handling
  - security_management

authentication:
  type: "oauth"  # Placeholder for future OAuth migration
  current: "api_key"
  env_var: "TOP_PRODUCER_API_KEY"

mcp_server:
  available: false
  source: "custom"

api_config:
  base_url: "https://api.topproducer.com"
  rate_limit: 1000  # requests per hour
  docs_url: "https://docs.topproducer.com"
  version: "v1"

deployment:
  dev:
    endpoint: "dev-top_producer-agent"
    project: "mapache-dev"
  staging:
    endpoint: "staging-top_producer-agent"
    project: "mapache-staging"
  prod:
    endpoint: "prod-top_producer-agent"
    project: "mapache-prod"

monitoring:
  health_check: true
  metrics: true
  logging: "structured_json"
  alerts: true

integration_status:
  implemented: false
  tested: false
  deployed: false
  production_ready: false