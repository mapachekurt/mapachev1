agent_name: "farm_at_hand_agent"
display_name: "Farm At Hand"
description: "Expert agent for Farm At Hand operations and integration"
version: "1.0.0"
model: "gemini-2.0-flash-exp"
tier: "Specialized Vertical Tools"
category: "agriculture"

capabilities:
  - api_integration
  - data_synchronization
  - workflow_automation
  - real_time_operations
  - error_handling
  - security_management

authentication:
  type: "oauth"  # Placeholder for future OAuth migration
  current: "api_key"
  env_var: "FARM_AT_HAND_API_KEY"

mcp_server:
  available: false
  source: "custom"

api_config:
  base_url: "https://api.farmathand.com"
  rate_limit: 1000  # requests per hour
  docs_url: "https://docs.farmathand.com"
  version: "v1"

deployment:
  dev:
    endpoint: "dev-farm_at_hand-agent"
    project: "mapache-dev"
  staging:
    endpoint: "staging-farm_at_hand-agent"
    project: "mapache-staging"
  prod:
    endpoint: "prod-farm_at_hand-agent"
    project: "mapache-prod"

monitoring:
  health_check: true
  metrics: true
  logging: "structured_json"
  alerts: true

integration_status:
  implemented: false
  tested: false
  deployed: false
  production_ready: false