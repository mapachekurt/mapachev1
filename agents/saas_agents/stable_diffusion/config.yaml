agent_name: "stable_diffusion_agent"
display_name: "Stable Diffusion"
description: "Expert agent for Stable Diffusion operations and integration"
version: "1.0.0"
model: "gemini-2.0-flash-exp"
tier: "Specialized Vertical Tools"
category: "ai"

capabilities:
  - api_integration
  - data_synchronization
  - workflow_automation
  - real_time_operations
  - error_handling
  - security_management

authentication:
  type: "oauth"  # Placeholder for future OAuth migration
  current: "api_key"
  env_var: "STABLE_DIFFUSION_API_KEY"

mcp_server:
  available: false
  source: "custom"

api_config:
  base_url: "https://api.stablediffusion.com"
  rate_limit: 1000  # requests per hour
  docs_url: "https://docs.stablediffusion.com"
  version: "v1"

deployment:
  dev:
    endpoint: "dev-stable_diffusion-agent"
    project: "mapache-dev"
  staging:
    endpoint: "staging-stable_diffusion-agent"
    project: "mapache-staging"
  prod:
    endpoint: "prod-stable_diffusion-agent"
    project: "mapache-prod"

monitoring:
  health_check: true
  metrics: true
  logging: "structured_json"
  alerts: true

integration_status:
  implemented: false
  tested: false
  deployed: false
  production_ready: false