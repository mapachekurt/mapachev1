agent_name: "clearbit_agent"
display_name: "Clearbit"
description: "Expert agent for Clearbit operations and integration"
version: "1.0.0"
model: "gemini-2.0-flash-exp"
tier: "Marketing & Sales"
category: "lead_generation"

capabilities:
  - api_integration
  - data_synchronization
  - workflow_automation
  - real_time_operations
  - error_handling
  - security_management

authentication:
  type: "oauth"  # Placeholder for future OAuth migration
  current: "api_key"
  env_var: "CLEARBIT_API_KEY"

mcp_server:
  available: false
  source: "custom"

api_config:
  base_url: "https://api.clearbit.com"
  rate_limit: 1000  # requests per hour
  docs_url: "https://docs.clearbit.com"
  version: "v1"

deployment:
  dev:
    endpoint: "dev-clearbit-agent"
    project: "mapache-dev"
  staging:
    endpoint: "staging-clearbit-agent"
    project: "mapache-staging"
  prod:
    endpoint: "prod-clearbit-agent"
    project: "mapache-prod"

monitoring:
  health_check: true
  metrics: true
  logging: "structured_json"
  alerts: true

integration_status:
  implemented: false
  tested: false
  deployed: false
  production_ready: false