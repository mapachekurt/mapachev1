agent_name: "prometheus_agent"
display_name: "Prometheus"
description: "Expert agent for Prometheus operations and integration"
version: "1.0.0"
model: "gemini-2.0-flash-exp"
tier: "Developer Tools"
category: "monitoring"

capabilities:
  - api_integration
  - data_synchronization
  - workflow_automation
  - real_time_operations
  - error_handling
  - security_management

authentication:
  type: "oauth"  # Placeholder for future OAuth migration
  current: "api_key"
  env_var: "PROMETHEUS_API_KEY"

mcp_server:
  available: false
  source: "custom"

api_config:
  base_url: "https://api.prometheus.com"
  rate_limit: 1000  # requests per hour
  docs_url: "https://docs.prometheus.com"
  version: "v1"

deployment:
  dev:
    endpoint: "dev-prometheus-agent"
    project: "mapache-dev"
  staging:
    endpoint: "staging-prometheus-agent"
    project: "mapache-staging"
  prod:
    endpoint: "prod-prometheus-agent"
    project: "mapache-prod"

monitoring:
  health_check: true
  metrics: true
  logging: "structured_json"
  alerts: true

integration_status:
  implemented: false
  tested: false
  deployed: false
  production_ready: false