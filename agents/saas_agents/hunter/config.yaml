agent_name: "hunter_agent"
display_name: "Hunter.io"
description: "Expert agent for Hunter.io operations and integration"
version: "1.0.0"
model: "gemini-2.0-flash-exp"
tier: "Marketing & Sales"
category: "lead_generation"

capabilities:
  - api_integration
  - data_synchronization
  - workflow_automation
  - real_time_operations
  - error_handling
  - security_management

authentication:
  type: "oauth"  # Placeholder for future OAuth migration
  current: "api_key"
  env_var: "HUNTER_API_KEY"

mcp_server:
  available: false
  source: "custom"

api_config:
  base_url: "https://api.hunter.com"
  rate_limit: 1000  # requests per hour
  docs_url: "https://docs.hunter.com"
  version: "v1"

deployment:
  dev:
    endpoint: "dev-hunter-agent"
    project: "mapache-dev"
  staging:
    endpoint: "staging-hunter-agent"
    project: "mapache-staging"
  prod:
    endpoint: "prod-hunter-agent"
    project: "mapache-prod"

monitoring:
  health_check: true
  metrics: true
  logging: "structured_json"
  alerts: true

integration_status:
  implemented: false
  tested: false
  deployed: false
  production_ready: false