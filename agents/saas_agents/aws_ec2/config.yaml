agent_name: "aws_ec2_agent"
display_name: "AWS EC2"
description: "Expert agent for AWS EC2 operations and integration"
version: "1.0.0"
model: "gemini-2.0-flash-exp"
tier: "Developer Tools"
category: "cloud"

capabilities:
  - api_integration
  - data_synchronization
  - workflow_automation
  - real_time_operations
  - error_handling
  - security_management

authentication:
  type: "oauth"  # Placeholder for future OAuth migration
  current: "api_key"
  env_var: "AWS_EC2_API_KEY"

mcp_server:
  available: false
  source: "custom"

api_config:
  base_url: "https://api.awsec2.com"
  rate_limit: 1000  # requests per hour
  docs_url: "https://docs.awsec2.com"
  version: "v1"

deployment:
  dev:
    endpoint: "dev-aws_ec2-agent"
    project: "mapache-dev"
  staging:
    endpoint: "staging-aws_ec2-agent"
    project: "mapache-staging"
  prod:
    endpoint: "prod-aws_ec2-agent"
    project: "mapache-prod"

monitoring:
  health_check: true
  metrics: true
  logging: "structured_json"
  alerts: true

integration_status:
  implemented: false
  tested: false
  deployed: false
  production_ready: false